import { Stack, Typography } from "@mui/material";
import { NextPage } from "next";

const EstatesList: NextPage = () => {

    return (
        <Stack>
            <Typography variant="h1" color="primary" fontWeight={'medium'}>
                اجاره باغ و ویلا در اصفهان
            </Typography>
        </Stack>
    )
}

export default EstatesList;